# Every command explicitly loads this file from the repository root.
# Copy it to .env, then fill in paths for the deployment control machine.

# Supported values: fuji, mainnet. Always call Fuji "Fuji", never "testnet".
NETWORK=fuji

# Optional override for the selected network's default P-chain API.
# PCHAIN_API=https://api.avax-test.network

# Funding secp256k1 key used for P-chain transactions and fee top-ups. Its
# derived EVM address receives the main L1's genesis allocation. There is no
# built-in funded account. This file is private and must not be committed.
PCHAIN_WALLET_KEY=staking/pchain-wallet.key

# Supported values: 1, 4. One is the simple default; four tolerates one lost
# signing key while retaining a 3-of-4 quorum.
MANAGER_COMMITTEE=1

# Fleet access from the deployment control machine.
SSH_USER=ubuntu
SSH_KEY_PATH=/path/to/fleet-key
