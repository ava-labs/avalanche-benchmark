apiVersion: 1

datasources:
    - name: Prometheus
      type: prometheus
      access: proxy
      # The compose service name. Change to http://localhost:9090 when
      # Grafana runs outside the compose network.
      url: http://prometheus:9090
      uid: prometheus
      isDefault: true
      editable: false
