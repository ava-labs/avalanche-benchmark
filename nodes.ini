# nodes.ini - the fleet inventory, single source of truth for node names,
# hosts and roles. One node per line:
#
#   <name> host=<ip> role=validator|rpc [dc=<tag>] [weight=<w>]
#
# - name is the PRIMARY KEY everywhere: fleet verbs (./fleet up a1), the
#   staking key dir (staking/l1/<name>), the node-ids.env manifest and the
#   node's data root on its box (data/<name>).
# - role: validator (registered L1 validator; a "spare" is just a validator
#   at weight 1) or rpc (tracks the chain, serves ingress, never registered,
#   never wears a BLS signer key).
# - dc is a display-only tag: `fleet status` groups by it and fleet verbs
#   accept dc=<tag> selectors. Nothing functional depends on it.
# - weight matters ONLY at `l1 create` time (the conversion's initial weight,
#   default 1). After creation the on-chain weight is the sole truth; the
#   values below document the ground state (site A active, site B standby).
# - Ports are positional per host: the k-th node on a host serves HTTP on
#   9652+2k and staking p2p on 9653+2k (one node per host = 9652/9653).
#   Reordering nodes that share a host shifts the later nodes' ports:
#   redeploy that host's nodes after such an edit.

# --- DC A (us-west-1) ---------------------------------------------------
a1     host=54.183.72.215   role=validator  dc=A  weight=100000
a2     host=18.145.46.27    role=validator  dc=A  weight=100000
a3     host=3.101.31.107    role=validator  dc=A  weight=100000
a4     host=18.144.206.214  role=validator  dc=A  weight=100000
rpc_a1 host=18.144.224.191  role=rpc        dc=A
rpc_a2 host=54.176.181.18   role=rpc        dc=A

# --- DC B (us-west-2) ---------------------------------------------------
b1     host=44.251.223.141  role=validator  dc=B  weight=1000
b2     host=34.217.28.121   role=validator  dc=B  weight=1000
b3     host=54.190.58.17    role=validator  dc=B  weight=1000
b4     host=54.149.198.170  role=validator  dc=B  weight=1000
rpc_b1 host=35.86.178.59    role=rpc        dc=B
rpc_b2 host=54.186.215.151  role=rpc        dc=B
