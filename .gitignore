# Local operator config and generated run metadata
/.env
**/.env
/network.env
/network-info.env

# Per-deploy secrets + manifest (generated by 00_gen_secrets.sh). NEVER commit:
# staking keys become Fuji validationID identities (leak = impersonation) and
# fuji-wallet.key holds real testnet funds / owns the L1.
/staking/

# Failover reconciler desired-state (per-deployment, regenerated by 03/up/down)

# Build/dependency artifacts
/bin/
/avalanchego
/benchmark
/bombard
/create-l1
/reconcile
/srEXiWaHuhNyGwPUi444Tu47ZEDwxTWrbQiuD7FmgSAQ6X7Dy
# Cross-compiled binaries (make pack)
/*-linux
/*-linux-amd64

# Deployment packages
/remote-benchmark.tar.gz
/avalanche-l1-kit.zip
# Secrets bundles (setup/03_backup_secrets.sh)
/secrets-backup-*.tar.gz
/avalanche-benchmark-*.tar.gz
/avalanche-benchmark-*.rpm
# RPM staging tree (make rpm)
/dist/

# Runtime data and logs
/data/
/runtime-data/
*.log
*.out
nohup.out

# Terraform local state/config
**/.terraform/
**/terraform.tfstate
**/terraform.tfstate.*
**/tfplan
**/*.tfvars
!**/*.tfvars.example
**/config.yaml
!**/config.yaml.example

# Demo-generated working dirs
/blockscout/
/multi-node/
/single-node/

# Local-only AWS demo harness (personal paths; never commit)
/two-dc-demo.sh

# Monitoring runtime (generated by run/02_monitoring.sh; bin/ binaries already ignored)
/monitoring/prometheus.yml
/monitoring/provisioning/

# Local editor/agent state
/.claude/
.DS_Store
.linux-build/

# benchmark-fleet operator state (per-deploy, never committed)
fleet-state.json
